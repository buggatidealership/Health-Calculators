**🎯 Goal:**
Ensure that `/resources/fertility-after-35` loads correctly in the browser without a 404 error. This requires fixing the missing route in `app.py`, verifying the correct filename exists in `templates/resources/`, and ensuring the sitemap and resource list are in sync.

---

### ✅ Fix Steps for Replit:

**1. Add Flask Route in `app.py`**
Below the existing article routes, add:
```python
@app.route('/resources/fertility-after-35')
def fertility_after_35():
    return render_template(
        'resources/fertility_after_35.html',
        is_homepage=False,
        schema_name="Fertility After 35: What to Know About Your Chances",
        schema_description="Evidence-based guide to fertility after age 35: age-related egg quality decline, success rates for natural conception, IVF outcomes, and pregnancy risks.",
        schema_url="/resources/fertility-after-35"
    )
```

**2. Confirm Template File Exists**
Verify that the template exists at:
```plaintext
templates/resources/fertility_after_35.html
```
✅ If not, rename or move the file accordingly.


**3. Update sitemap.xml (if not already)**
Ensure the `<url>` block for this page is present only **once**:
```xml
<url>
  <loc>https://longevitycalculator.xyz/resources/fertility-after-35</loc>
  <priority>0.7</priority>
  <changefreq>monthly</changefreq>
  <lastmod>2025-04-03</lastmod>
</url>
```

✅ If it appears twice: remove the duplicate.
✅ Ensure file starts with `<?xml` on line 1 (no blank line before).

**4. Restart Flask App and Confirm**
Once the above is complete:
- Visit: https://longevitycalculator.xyz/resources/fertility-after-35
- Confirm the article loads with correct content.
- Confirm it's indexed in `/sitemap.xml`
- Confirm it's clickable from `/resources`

---

### Final QA Checklist ✅
- [ ] Route `/resources/fertility-after-35` returns 200
- [ ] File `fertility_after_35.html` exists in `/templates/resources/`
- [ ] Metadata renders in `<head>`
- [ ] Open Graph image appears on share
- [ ] Article is in sitemap.xml
- [ ] Article card appears on `/resources`

Once confirmed, close the loop with QA on sitemap fetch issue in Google Search Console.