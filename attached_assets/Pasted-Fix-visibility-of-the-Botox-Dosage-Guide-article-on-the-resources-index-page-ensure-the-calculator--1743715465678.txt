Fix visibility of the Botox Dosage Guide article on the resources index page, ensure the calculator link points to the correct destination, and sync sitemap.xml and navigation links.

---

**1. Make Botox Dosage Guide appear on /resources**
- Go to the template rendering the `/resources` page (likely `resources.html` or a Jinja loop).
- Ensure the file `/templates/resources/botox-dosage-guide.html` is being picked up.
- If articles are **manually listed**, add this:
```html
<li>
  <a href="/resources/botox-dosage-guide">Botox Dosage Guide</a>
</li>
```
- If using a loop, confirm that the file is not excluded due to filtering logic (e.g., alphabetical sort or file extension mismatch).

---

**2. Fix all calculator links on the article page**
- Open `botox-dosage-guide.html`
- Replace **all** instances of:
```html
href="/botox-calculator"
```
With:
```html
href="/botox-dosage-calculator"
```
- This includes:
  - CTA button links
  - Inline text links
  - FAQ section links
  - Calculator summary cards

---

**3. Update sitemap.xml**
- Add the following entry to `sitemap.xml`:
```xml
<url>
  <loc>https://longevitycalculator.xyz/resources/botox-dosage-guide</loc>
  <lastmod>2025-04-03</lastmod>
  <changefreq>monthly</changefreq>
  <priority>0.80</priority>
</url>
```
- Confirm the sitemap is valid XML, starts with a clean declaration, and does not include BOM or whitespace above `<?xml version="1.0" encoding="UTF-8"?>`.

---

**4. Patch internal navigation (header/footer)**
- In `header.html` and `footer.html`, add an entry to the resources dropdown or sidebar:
```html
<li><a href="/resources/botox-dosage-guide">Botox Dosage Guide</a></li>
```
- If using a nav macro, ensure it's dynamically pulling the new file.

---

**5. QA Checks After Fix:**
- ✅ Confirm `/resources/botox-dosage-guide` is visible in `/resources`
- ✅ Confirm **every** calculator link goes to `/botox-dosage-calculator`
- ✅ Confirm sitemap is updated and loads without error
- ✅ Confirm nav/footer link appears correctly
- ✅ Confirm canonical tags and internal links resolve