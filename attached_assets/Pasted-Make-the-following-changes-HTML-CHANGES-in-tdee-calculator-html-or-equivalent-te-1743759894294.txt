Make the following changes:

---

### ✅ HTML CHANGES (in `tdee_calculator.html` or equivalent template)

Replace the existing unit selector block (currently using radio buttons) with the following HTML:

```html
<div class="units-toggle">
  <button id="metric-btn" class="active" onclick="toggleUnits('metric')">Metric (kg, cm)</button>
  <button id="imperial-btn" onclick="toggleUnits('imperial')">Imperial (lbs, ft/in)</button>
</div>
```

Ensure this block appears just above the Gender field, following the layout of the Caloric & Macronutrient Calculator.

---

### ✅ CSS STYLES

If not already included, add the following CSS (either inline or in your styles file):

```css
.units-toggle {
  display: flex;
  gap: 10px;
  margin-bottom: 20px;
}

.units-toggle button {
  flex: 1;
  padding: 10px;
  border: 1px solid #ccc;
  border-radius: 8px;
  background-color: #f0f0f0;
  color: #333;
  font-weight: 500;
  cursor: pointer;
  transition: background-color 0.2s ease;
}

.units-toggle button.active {
  background-color: #d63384;
  color: white;
  border-color: #d63384;
}

.units-toggle button:hover {
  background-color: #eee;
}
```

---

### ✅ JAVASCRIPT LOGIC

Ensure the existing toggle function adds/removes the `active` class correctly:

```js
function toggleUnits(unit) {
  const metricBtn = document.getElementById('metric-btn');
  const imperialBtn = document.getElementById('imperial-btn');

  if (unit === 'metric') {
    document.getElementById('metric-weight').style.display = 'block';
    document.getElementById('imperial-weight').style.display = 'none';
    document.getElementById('metric-height').style.display = 'block';
    document.getElementById('imperial-height').style.display = 'none';

    metricBtn.classList.add('active');
    imperialBtn.classList.remove('active');
  } else {
    document.getElementById('metric-weight').style.display = 'none';
    document.getElementById('imperial-weight').style.display = 'block';
    document.getElementById('metric-height').style.display = 'none';
    document.getElementById('imperial-height').style.display = 'block';

    imperialBtn.classList.add('active');
    metricBtn.classList.remove('active');
  }
}
```

---

### 🔁 CLEANUP
- Remove or comment out any existing radio button HTML and old styling associated with the previous unit selector.

---

Once complete, the unit toggle should match the UI/UX of `/caloric-intake-macronutrient-calculator` 1:1.

Let me know when it's pushed and I’ll do a full visual + functional QA.
